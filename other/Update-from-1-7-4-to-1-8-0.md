# Инструкция по обновлению с версии 1.7.4 до версии 1.8.0

## ⚠️ Важные рекомендации

**Рекомендуется выполнить полное удаление и новую установку ноды!** Это самый надежный способ избежать проблем с совместимостью.

Если полное удаление невозможно, следуйте инструкции ниже.

## 🔄 Пошаговая инструкция обновления

### 1. Обновите скрипт до последней версии
```bash
curl -o install_sepolia.sh https://raw.githubusercontent.com/pittpv/sepolia-auto-install/main/install_sepolia.sh && chmod +x install_sepolia.sh && ./install_sepolia.sh
```

### 2. Перейдите в папку ноды
```bash
cd /root/sepolia-node/
```

### 3. Отредактируйте docker-compose.yml
Лучше сделать это во внешнем редакторе, в ином случае используйте `nano`

Откройте файл для редактирования:
```bash
nano /root/sepolia-node/docker-compose.yml
```

**Удалите следующие секции:**

**Для execution клиента (geth/reth/nethermind):**
```yaml
    deploy:
      resources:
        limits:
          memory: 18G
          cpus: '4.0'
        reservations:
          memory: 12G
          cpus: '4.0'
```

**Для consensus клиента (lighthouse/prysm/teku):**
```yaml
    deploy:
      resources:
        limits:
          memory: 8G
          cpus: '2.0'
        reservations:
          memory: 4G
          cpus: '2.0'
```

**Убедитесь, что изменения сохранились!**

### 4. Удалите файл конфигурации ресурсов
```bash
rm -f /root/sepolia-node/resource_config.env
```

### 5. Удалите папку consensus клиента
Выполните команду соответствующую вашему consensus клиенту:

**Для Prysm:**
```bash
rm -rf /root/sepolia-node/prysm/
```

**Для Lighthouse:**
```bash
rm -rf /root/sepolia-node/lighthouse/
```

**Для Teku:**
```bash
rm -rf /root/sepolia-node/teku/
```

### 6. Проверьте версию скрипта
Запустите обновленный скрипт управления и убедитесь, что отображается версия **1.8.0**

### 7. Настройте новые лимиты ресурсов
В меню скрипта выберите опцию **15** и установите новые лимиты ресурсов.

**В ответ на запрос введите:** `yes`

### 8. Обновите ноду (рекомендуется)
Если давно не обновляли ноду, выполните опцию **3** для обновления контейнеров.

### 9. Настройте мониторинг
Настройте уведомления в Telegram (опция 6) и следите за дистанцией в beacon chain в уведомлении.

## 📊 Минимальные требования системы

- **RAM:** 24 GB
- **CPU:** 8 ядер
- **Storage:** SSD с достаточным свободным местом
